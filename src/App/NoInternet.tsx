import React, { ReactElement } from 'react'
import { View } from 'react-native'

// Stubbed NoInternet for Expo POC - the component was already disabled (false &&)
const NoInternet = (): ReactElement => {
  return <></>
}

export default NoInternet
